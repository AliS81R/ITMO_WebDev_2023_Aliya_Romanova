const OPERATIONS = {
  sum: { method: (a, b) => a + b, title: "+" },
  sub: { method: (a, b) => a - b, title: "-" },
  mult: { method: (a, b) => a * b, title: "*" },
  divd: { method: (a, b) => a / b, title: "/" },
};

export { OPERATIONS };
